<?xml version="1.0" encoding="utf-8" ?>

<component name="ExitScreen" extends="Group" initialFocus="ExitButtons">

    <interface>
        <!-- Button press handler -->
        <field id="exitItemSelected" type="integer" alwaysnotify="true" alias="ExitButtons.itemSelected" onChange="OnExitItemSelected" />

        <!-- exit flag -->
        <field id="exitFlag" type="boolean" />

        <!-- cancel flag -->
        <field id="cancelFlag" type="boolean" />

        <!-- focusedColor setting -->
        <field id="buttonsFocusedColor" type="string" alias="ExitButtons.focusedColor"/>

        <!-- font path -->
        <field id="font" type="string"/>
    </interface>

    <!-- main handler -->
    <script type="text/brightscript" uri="pkg:/components/screens/ExitScreen/ExitScreen.brs" />

    <children>

        <Poster
            translation="[0,0]"
            uri="pkg:/images/menu_background1.jpg"
            width="1280"
            height="720" />

        <Rectangle
            translation="[0,0]"
            width="1280"
            height="720"
            color="0x000000"
            opacity="0.72"
        />

        <!-- Exit buttons -->
        <LabelList
            id = "ExitButtons"
            vertFocusAnimationStyle = "floatingFocus"
            drawFocusFeedback = "true"
            drawFocusFeedbackOnTop = "false"
            focusBitmapUri = "pkg:/Images/focus_button.9.png"
            focusFootprintBitmapUri="pkg:/Images/focus_empty_square.png"
            color = "0xFFFFFFFF"
            focusedColor = "0xE40090FF"
            textHorizAlign = "left"
            translation = "[850,200]"
            itemSize = "[150, 80]"
            itemSpacing = "[0,60]">

            <ContentNode role = "content">
                <ContentNode title = " Exit" />
                <ContentNode title = " Stay"   />

            </ContentNode>

        </LabelList>

        <!-- Overhang -->
        <Poster
 	        translation="[65, 36]"
            uri="pkg:/images/overhangLogo.png"
            width="321"
            height="56"
        />

    </children>
</component>
