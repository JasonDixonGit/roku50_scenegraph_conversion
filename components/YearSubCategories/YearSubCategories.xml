<component name="YearSubCategories" extends="Group" initialFocus="CategoryList">
    <interface>

        <!-- Grid Content Nodes For Each Row -->
        <field id="content" type="node" alias="CategoryList.content" onChange="onContentChange"/>

        <!-- Row item selection handlers -->
        <field id="YearListItemSelected" type="intarray" alwaysnotify="true" alias="YearList.rowItemSelected"/>

        <!-- Row item focused handler 2 -->
        <field id="itemFocused" type="intarray" alias="YearList.rowItemFocused" />

        <!-- Row item focused handler -->
        <field id="lastItemFocus" type="intarray"/>

        <!-- Interface to focused item (Content Node) -->
        <field id="focusedContent"  type="node"/>        

        <!-- secondaryColor settings -->
        <field id="YearListLabelColor" type="string" alias="YearList.rowLabelColor"/>

        <!-- global font -->
        <field id="font" type="string" />

    </interface>

    <!-- main handler -->
    <script type="text/brightscript" uri="pkg:/components/YearSubCategories/YearSubCategories.brs" />



    <children>
        <FadingBackground
            id="Background"
            width="1280"
            height="720"
            color="0x000000"
            ShadeOpacity="0.72"
        />

        <RowList
            id="YearList"
            focusBitmapUri="pkg:/images/focus_button.9.png"
            translation="[60, 300]"
            itemSize="[1400, 708]"
            numRows="1"
            itemSpacing="[20, 0]"
            focusXOffset="[0]"
            vertFocusAnimationStyle="fixedFocus"
            rowFocusAnimationStyle="fixedFocusWrap"
            rowItemSize="[[394,276]]"            
            rowItemSpacing="[[22, 2]]"
            rowLabelFont="font:LargeSystemFont"
            showRowLabel="true"
            rowLabelColor="0xFFFFFFFF"
            showRowCounter="true"
            rowLabelOffset="[[0, 30]]"
        />

        <Poster
            translation="[0, 650]"
            uri="pkg:/images/BG_dark_down.png"
            width="2000"
            height="95"
        />

        <Description
            id="Description"
            translation="[65, 120]"
            itemSpacings="[7,17]"
        />

    </children>
</component>